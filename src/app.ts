const myString: string = 'Eirik';

console.log(myString.toUpperCase());

let num: number = 20;

num = num * 2;

console.log(num);

const apple: boolean = true;

if (apple) {
  console.log('apple');
}

interface Person {
  name: string;
  skin: string;
  male: boolean;
}

const user: Person = {
  name: 'Eirik',
  skin: 'caucation',
  male: true,
};

console.log(user);
